
  # Mobile-first School Website UI

  This is a code bundle for Mobile-first School Website UI. The original project is available at https://www.figma.com/design/amPmwpVHsJfLOnBepRa7ET/Mobile-first-School-Website-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  